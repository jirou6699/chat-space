.MessageBox{data: {message: {id: message.id}}}
  .MessageInfo
    .MessageInfo--name
      = message.user.name
    .MessageInfo--date
      = message.created_at.strftime("%Y年%m月%d日 %H時%M分")
  .MessageText
    -if message.text.present?
      %p.MessageText__content
        = message.text
    = image_tag message.image.url, class: 'MessageText__image' if message.image.present?